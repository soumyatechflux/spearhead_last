/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['techfluxsolutions.com'], // Make sure this domain is correctly added
  }
  
}

module.exports = nextConfig
